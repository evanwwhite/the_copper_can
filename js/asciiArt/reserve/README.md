# Protected Asset Reserve

Artwork placed here is intentionally retained for future development. Do not
delete it based on current runtime usage. Prefer subdirectories named
`characters`, `enemies`, `environments`, `interiors`, `worldLocations`,
`interface`, `animations`, and `unidentified` as the reserve grows.

Moving existing art here is optional when a move could alter its bytes or make
its history harder to follow.

